# API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# ML inference — scikit-learn 1.5.x gerekli (1.4.2 ile egitilmis pkl uyumlulugu)
scikit-learn==1.5.2
xgboost>=2.0.0
# Ortak conda base'te contourpy/streamlit/pywavelets (numpy<2); izole `.venv` hâlâ en temiz çözüm
numpy>=1.26.0,<2.0
pandas>=2.0.0
pyarrow>=12.0.0
torch>=2.0.0

# Opsiyonel: SHAP aciklama endpoint'i icin
shap>=0.42.0

# Offline egitim + Adim10 grafikleri (enhance_preprocessing, train_smote_models)
matplotlib>=3.7.0
seaborn>=0.13.0
imbalanced-learn>=0.12.0